const objRectancle = {
    width : 5,
    height : 5,


    calcArea(){
        return area = this.width * this.height;
    },

    calcPeri(){
        return perimter = (2*this.height) + (2*this.width);
    } 
}


console.log(objRectancle.calcArea())

console.log(objRectancle.calcPeri())